apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datalbus.fullname" . }}-test-connection"
  labels:
    {{- include "datalbus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datalbus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
